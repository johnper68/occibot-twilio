import { ProviderClass } from '@bot-whatsapp/bot';
import { Vendor } from '@bot-whatsapp/bot/dist/provider/providerClass';
import { Button } from '@bot-whatsapp/bot/dist/types';
import twilio from 'twilio';

import { TwilioWebHookServer } from './server';
export interface ITwilioProviderOptions {
    accountSid: string;
    authToken: string;
    vendorNumber: string;
    port?: number;
    publicUrl?: string;
}
export interface IMessageOptions {
    buttons?: Button[];
    media?: string;
}
declare class TwilioProvider extends ProviderClass {
    twilioServer: TwilioWebHookServer;
    protected vendor: Vendor<twilio.Twilio>;
    private vendorNumber;
    private publicUrl;
    constructor({ accountSid, authToken, vendorNumber, port, publicUrl, }: ITwilioProviderOptions);
    private busEvents;
    private sendMedia;
    private sendButtons;
    sendMessage(number: string, message: string, arg: {
        options?: IMessageOptions;
    }): Promise<any>;
}
export { TwilioProvider };
//# sourceMappingURL=index.d.ts.map